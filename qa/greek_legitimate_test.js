// ✅ LEGITIMATE GREEK - Should NOT flag (0 findings)

// Pure Greek variable names
const μήνυμα = "hello";  // Greek for "message"
const α = 1;  // Common math variable
const β = 2;
const γreek_sum = α + β;

// Greek in comments (legitimate)
// ελληνικά σχόλια
// Ελληνικά κείμενα

// Greek function names
function υπολογισμός(α, β) {
    return α + β;
}

// Mathematical notation
const θ = Math.PI / 2;
const φ = (1 + Math.sqrt(5)) / 2;  // Golden ratio
const Δ = b * b - 4 * a * c;  // Discriminant

// Pure Greek identifiers (legitimate i18n)
const καλημέρα = "good morning";
const κόσμε = "world";

// Greek letters in pure form (common in math/science)
const π = Math.PI;
const ε = 0.001;  // Epsilon
const δ = 0.0001; // Delta
const Σ = (a, b) => a + b;  // Sigma (summation)
